description = "Switch SecretKeeper intensity level (lite/full/ultra/off)"
prompt = "Switch to SecretKeeper {{args}} mode. If no level specified, use full. Before any code: never hardcode API keys, passwords, JWT secrets, OAuth credentials, private keys, or database connection strings. Use environment variables. Update .env.example with placeholders. Run the scanner if unsure."
