description = "Scan current git diff for leaked secrets (deterministic scanner)"
prompt = "Run the SecretKeeper scanner on the current git diff. Execute: node bin/secretkeeper.js scan (or node hooks/secretkeeper-scanner.js if from plugin root). Report every finding with file, line, severity, and recommended fix (env var name + .env.example entry). In full/ultra mode, fix any findings automatically."
