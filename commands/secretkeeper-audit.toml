description = "Audit entire repository for leaked secrets"
prompt = "Run a full SecretKeeper repo audit. Execute: node bin/secretkeeper.js audit. Report all findings grouped by severity (critical first). For each: file, line, secret type, redacted match, and fix. Offer to auto-fix hardcoded secrets with env var references. Respect .secretkeeperignore."
